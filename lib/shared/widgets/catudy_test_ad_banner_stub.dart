import 'package:flutter/widgets.dart';

class CatudyTestAdBanner extends StatelessWidget {
  const CatudyTestAdBanner({required this.show, super.key});

  final bool show;

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
