

## Commands

```powershell
flutter pub get
flutter test
flutter run

# Online MVP with Supabase (do not commit keys)
flutter run --dart-define=SUPABASE_URL=https://your-project.supabase.co --dart-define=SUPABASE_ANON_KEY=your-anon-key

# Apply Supabase SQL before online testing:
# supabase/catudy_complete.sql
```
